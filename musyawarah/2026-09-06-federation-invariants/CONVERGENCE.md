# CONVERGENCE — 2026-09-06-federation-invariants

Parties: architect-333 (sealed first) vs auditor-555 (sealed blind). Disagreement recorded, never hidden.

## AGREEMENTS (both parties)
1. Invariant SET ratified. Auditor verdict RATIFY-WITH-CHANGES; architect accepts all 9 changes.
2. Split-brain membership test accepted WITH auditor amendment: add consequence clause (what breaks when nodes disagree) + loudness clause (does divergence fail-scream or fail-silent) + periodic re-run, else the test admits everything and rots.
3. Auditor found the set UNDER-COUNTED (OBS): KVM4 arifOS clone 8-days stale + near-empty VAULT999 copy, 25 unmatched ports (not 12), ghost doctrine pointer in /root/AGENTS.md. Fragment amended by reference below.
4. W0 template falsified as written — present in tool responses + state.json, ABSENT in /health. Corrected shape: "every TOOL response + state surface", enumerated, not "every response".
5. No canon promotion without F13 token. Fragment stays DRAFT until Arif rules.

## DISAGREEMENT + RESOLUTION
Architect sequenced enforcement by live-risk: H-WELL attestation → boot-parity → receipt-gate.
Auditor sequenced by dependency: receipts are the substrate; attestation before witnesses exist = Q9 self-certification moved up a layer; boot-parity without receipt-informed role-awareness = availability bomb on KVM4's intentional workshop-stale clone.

RESOLUTION: auditor wins on evidence (DER from OBS probes; architect position was INT). Converged sequence:
  1. Receipt-gate SOFT epoch (warn-only, no rejects) + G-WELL witness-plane feed — tied first
  2. Role-aware boot-parity (workshop-stale ≠ running-fork; baseline from epoch-1 receipts)
  3. Signed attestation fan-out LAST — F13-rooted keys, only after witnesses exist to counter-sign
Architect pre-registered defense was answered, not averaged. Position files unchanged (sealed).

## BINDING CHANGES TO FRAGMENT (apply on promotion, not before)
- W0 surface enumeration (tool+state, not every-response)
- Role-aware parity manifest (per-node role field from MACHINE_MAP ruling)
- Two-epoch receipt gate (warn 48h → reject; FQ death-spiral guard)
- Under-counted items folded: stale-clone disposition, port reconciliation 25, ghost pointer fix
