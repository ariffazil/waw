# The Zen Card — arifOS in One Problem, Three Keepables, Six Capabilities · DRAFT

> **Status:** F13-RATIFIED via chat directive 2026-09-07 ("seal all"). Classifier v1 locked + deployed (FED v3.3.0-zen-classifier, verified live). E11 "Proposal First" adopted as agent-interface norm.
> **Author:** FI-008, commissioned by 888: *"zen all."*
> **Siblings:** `FED-EUREKA-DISTILLATION-2026-09-07.md` (v0.4 closure) · `ZEN-BIJAKSANA-ARIF-RUNTIME-MAP-2026-09-07.md`.

---

## 1 · The one problem

Every component — LLM, memory, MCP, skills, agents, routing, tools — solves one problem:

```text
How do we convert intelligence into useful, repeatable capability?
```

In arifOS terms: **Attention → Capability → Consequence.** Not wise answers — reliable, repeatable outcomes that survive vendor death.

## 2 · The three keepables (and the real dividing line)

If only three things could be saved: **Identity · Memory · Capability Graph.**

```text
Identity   answers: Who am I?              (i-ARIF / i-AZWA cards)
Memory     answers: What persists?          (helix / witness / VAULT999)
Capability answers: What can become real?   (CAPABILITY_GRAPH.json, sealed)
```

Everything else — LiteLLM, FED, Skills, MCP, tools — is infrastructure: important, replaceable. The dividing line is not size or cleverness; it is **consequence-bearing**. *What survives is what bears consequence. What can be swapped is what carries tokens.* Infrastructure dies and is reborn with vendors; the three keepables are the organism. (Aligns: SOUL ≠ model; scars = semantic mass; unforgeable-edge — only the sovereign draws possibility→reality.)

## 3 · The stack, zen-defined (ratified wording)

| Layer | Zen | Answers |
|---|---|---|
| Identity cards | Self (sovereign-owned) | Who am I? |
| Kernel F1–F13 | Law | What is not violable? |
| AAA | Mind (judgment organ) | Which possibility is chosen? |
| FED | Nervous system (identity-blind) | Who executes? |
| Capability Graph | Constitutional primitive | What can be done? |
| Skills | Compressed experience | How to start ahead? |
| Memory (helix/witness) | Continuity, not recall | What persists? |
| LiteLLM | Adapter | How to reach models? |
| MCP | Hands and eyes | How to touch reality? |
| A-FORGE | Hands (governed) | Make it happen |

## 4 · Capability Classifier v1 — LOCKED

Deterministic, one question only: *"capability apa diperlukan?"* — never identity, never emotion, never philosophy. Output `{capability, confidence}`. Six classes, routing table **corrected against live SOT** (essay's `flame-free` was stale — FLAME retired 2026-09-04, F13-acked; free lane = token-plan/flash lane):

| Class | Route (live-corrected) | Status |
|---|---|---|
| conversation | **NEW SOT signature** `fed-conversational` → token-plan free lane (mimo-v2.5 / kimi lane) | add at build |
| vision | `fed-multimodal-vision` | exists ✅ |
| reasoning | `fed-reasoning-heavy` → k3-class | exists ✅ |
| coding | **NEW SOT signature** `fed-coding` → deepseek-v4-pro family | add at build |
| long_context | `fed-long-context` → 1M-ctx models (essay's `k3-256k` is not a signature — corrected) | exists ✅ |
| action | `fed-agent-subagent` | exists ✅ |

Placement (final, v0.3–v0.4 rulings): **function inside FED** + `fed_classify` verb. Identity parallel via SOUL_STAMP, never serial. Prerequisite P0.2 (health honesty — two corpse-routes witnessed). "90% of messages fall in six classes" = hypothesis; the acceptance test measures the real distribution via telemetry.

## 5 · The chain (final — unchanged from v0.4)

```text
Mesej ──┬── (selari) SOUL_STAMP ← kad TERSEGEL  (siapa menjawab)
        ↓
     FED fed_route(task, agent_id)              (classifier μs + rank + cascade)
        ↓
     Model (prompt berstempel) → Jawapan        [AAA review only ≥666]
```

## 6 · Closure

Five essays, one convergence: **don't rebuild what exists (cards, memory, skills, floors); build the one missing thing (classifier + honest tables); keep identity out of routing; keep three keepables sovereign.**

Deliberation closed. Next motion is code: **P0.2 → classifier + `fed_classify` + 2 SOT signatures → WawaBot shim → acceptance test.**

## 7 · Phase 0 EXECUTED — 2026-09-07 03:54 MYT (FI-008, verified live)

| Fix | Status | Evidence |
|---|---|---|
| 1. Probe→health | ✅ code + data | `fed_router.py` gate falls back to `probe` row; notes hard-markers (DEAD/DO NOT ROUTE/DRAINED) filter; corpses flipped: qwen-team DEAD, qwen-individual RATE_LIMITED (reset 09-11), opencode-go/zen DEAD; minimax/deepseek probe rows corrected LIVE. New canonical writer: `AAA/scripts/fed_health_set.py` |
| 2. SOT prune | ✅ via health | DEAD skip = effective prune with revival path intact; literal SOT route removal deferred (unnecessary while gate is honest) |
| 3. Telemetry callers | ✅ ingress live | `POST :7074/report` (same logic as fed_report_latency); first real sample recorded (deepseek n=6→7). Middleware/harness caller wiring = next session |
| 4. task load-bearing | ✅ classifier v1 | `classify_capability()` + `fed_classify` verb + auto-apply (conf ≥0.90; vision ≥0.75); 2 signatures added to SOT (`fed-conversational`, `fed-coding` → 15 total); SOT backed up `*.bak-pre-classifier-20260907` |

**Live verification:** "check gambar ni pls" → auto `fed-multimodal-vision`, rank 1 qwen-vl-max. Default reasoning task → qwen-team ABSENT from ranks (was #2), individual demoted last-resort with RATE_LIMITED flag. Service: `fed-router.service` v3.3.0-zen-classifier, active, compile clean. Classifier unit tests: 6/6 classes correct incl. BM ("gambar", "cari", "terangkan").

**Remaining:** WawaBot shim (~20 lines, passes task + `fed-conversational` on conversation class) → acceptance test; middleware post-call → `/report` wiring; `epistemic_profiles` **awaits F13 with CORRECTED task-class schema** — the proposed arif/azwa identity-weight table is rejected (identity conflation; azwa numbers contradicted the i-AZWA card). Uncommitted (Z7 bundle).

*DITEMPA BUKAN DIBERI — menunggu F13.*
